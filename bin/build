#!/usr/bin/env bash
set -o errexit
set -o nounset
# set -o xtrace
set -o pipefail

rm -rf dist

rollup -c
html-minifier -c node_modules/html-minifier/sample-cli-config-file.conf index.html -o dist/index.html
gzip --best --keep dist/*

touch dist/Staticfile
echo '{"root": "./"}' > dist/static.json
